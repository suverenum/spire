/**
 * This demo script lives at tempo-mppx/demo.ts
 *
 * Run it from the tempo-mppx directory:
 *   cd tempo-mppx && npx tsx demo.ts
 *
 * It must run from tempo-mppx because it imports from the mppx source directly.
 * See tempo-mppx/demo.ts for the full implementation.
 */
console.error("Run this demo from the tempo-mppx directory:");
console.error("  cd tempo-mppx && npx tsx demo.ts");
process.exit(1);
